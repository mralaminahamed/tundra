# Tundra docker bundle — files never committed
# Author: Al Amin Ahamed (@mralaminahamed)

# Per-stack environment files (templates are checked in)
**/.env
!**/.env.example

# Production secrets — generated by prod/scripts/generate-secrets.sh
prod/secrets/

# Local backup output
prod/backups/

# OS noise
.DS_Store
Thumbs.db
